/**
 * Static Sydney CBD road data - 353 road segments
 * Used as fallback when Overpass API fails or times out.
 * TODO: Populate with real Sydney CBD road geometry from OSM export.
 */
export const sydneyRoads = [
  // Placeholder - will be populated with real Sydney CBD road data
  // Each entry: { id, classification, geometry: [[lon, lat], ...], length, name }
];
